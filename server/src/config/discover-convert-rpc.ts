/**
 * Discovery session → audit conversion RPC contract (Postgres function name + application error codes).
 */

export const DISCOVER_CONVERT_SESSION_RPC = 'discovery_convert_session_atomic' as const;
