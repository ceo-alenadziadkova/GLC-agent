/** Re-export shared sentinel URL + helpers from `@glc/intake-core` (single source of truth). */
export {
  NO_PUBLIC_WEBSITE_URL,
  formatAuditWebsiteDisplay,
  isNoPublicWebsiteUrl,
} from '@glc/intake-core';
