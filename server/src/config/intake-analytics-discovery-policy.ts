/**
 * Stable JSON key for experiment variant on `intake_analytics_events.intake_versions` (public discovery funnel).
 * Keep aligned with analytics consumers; do not rename without a migration plan.
 */
export const INTAKE_ANALYTICS_INTAKE_VERSIONS_EXPERIMENT_VARIANT_KEY = 'experimentVariant' as const;
