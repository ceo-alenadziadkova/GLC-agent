/**
 * Pipeline phase index bounds for full audits (recon = 0, strategy = 7).
 * Retry/start routes still constrain per-audit caps via `maxPhaseForMode`.
 */

export const PIPELINE_MIN_PHASE = 0;
export const PIPELINE_MAX_PHASE_INDEX = 7;
