/**
 * Short LLM prompt fragments (English). Edit JSON only; import from services, not inline literals.
 */

import raw from './prompt-fragments.en.json' with { type: 'json' };

export const CONTEXT_BUILDER_NO_PUBLIC_WEBSITE_LINE = raw.contextBuilderNoPublicWebsiteLine;
