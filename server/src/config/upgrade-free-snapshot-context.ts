/**
 * English copy and heuristics for free-snapshot → paid upgrade prefill (`upgradeFreeSnapshotAudit`).
 * Edit JSON for wording; tune numeric limits in `SYSTEM_DEFAULTS.upgradeFreeSnapshotPrefill`.
 */

import raw from './upgrade-free-snapshot-context.en.json' with { type: 'json' };

export const UPGRADE_FREE_SNAPSHOT_CONTEXT_EN = raw;
