/**
 * PDF Generator — public entry re-export.
 *
 * Implementation lives in `./pdf-generator/` (see `pdf-generator/index.ts`).
 */

export { PdfGenerator, pdfGenerator } from './pdf-generator/index.js';
