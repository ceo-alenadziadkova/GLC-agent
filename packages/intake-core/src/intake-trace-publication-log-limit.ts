/**
 * Default `limit` for GET intake trace wording publication log (consultant tooling).
 * Must match the Zod default on the server and the SPA request query string.
 */
export const INTAKE_TRACE_PUBLICATION_LOG_DEFAULT_LIMIT = 40 as const;
