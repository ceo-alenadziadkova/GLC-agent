/**
 * Timing defaults for Discover wizard UX (scroll, save-session race timeout).
 */

export const DISCOVER_QUESTION_SCROLL_DELAY_MS = 80;

/** Max wait when persisting discovery session before showing results without a token */
export const DISCOVER_WIZARD_SAVE_TIMEOUT_MS = 5000;
