/**
 * Shared legal UI strings (e.g. “Back to sign in”) for public legal pages.
 * Document bodies live in `*-page.en.ts` / `*-page.es.ts` modules.
 */

export const LEGAL_DOC_PAGES_EN = {
  backToLogin: 'Back to sign in',
} as const;
