/**
 * Subset of comparison rows for Focus / Context package pages (full matrix stays on Strategy + home compact).
 */
export const AUDIT_COMPARE_FOCUSED_ROW_LABELS = {
  starter: [
    'Coverage breadth',
    'Speed to first focused actions',
    'Roadmap depth and sequencing',
    'Handoff-ready for another team',
  ],
  pro: [
    'Coverage breadth',
    'Cross-domain dependency visibility',
    'System-wide comparability of score',
    'Roadmap depth and sequencing',
  ],
} as const;
