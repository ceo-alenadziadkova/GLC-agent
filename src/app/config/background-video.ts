/** Login (and other explicit surfaces): ambient loop + overlay strength. Marketing shell uses plain `--bg-canvas` only. */
export const BACKGROUND_VIDEO_CONFIG = {
  src: '/media/background.webm',
  overlayOpacity: 0.38,
} as const;
