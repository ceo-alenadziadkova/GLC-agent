/**
 * Pipeline monitor page copy (JSON CMS-style layer).
 */

import copy from '../locales/en/pipeline-monitor-copy.en.json';

export const PIPELINE_MONITOR_COPY = copy;

export type PipelineMonitorCopy = typeof copy;
