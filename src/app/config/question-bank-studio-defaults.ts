/**
 * Question Bank Studio visibility default. Wired through `app-feature-flags.ts`.
 */

export const QUESTION_BANK_STUDIO_ENABLED_DEFAULT = true;

/** Search / custom JSON debounce in Question Bank Studio (ms). */
export const QUESTION_BANK_STUDIO_DEBOUNCE_MS = 400;
