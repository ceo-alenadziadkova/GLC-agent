/**
 * Central localStorage keys for cross-page flows. Keep names stable; migrating values requires a versioned key.
 */

/** Persists public Discovery session token between marketing and post-login `/audit/new`. */
export const GLC_DISCOVERY_SESSION_TOKEN_STORAGE_KEY = 'glc_discovery_token';
