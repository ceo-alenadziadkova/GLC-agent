/**
 * Pre-brief context field ids shown as attachment nodes on the Studio graph (not bank question ids).
 */
export const STUDIO_PRE_BRIEF_IDENTITY_FIELD_IDS = [
  'a11',
  'a12',
  'a2',
  'intake_industry_specify',
] as const;
