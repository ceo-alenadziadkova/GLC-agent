/**
 * Technical defaults for the phase result editor (not user-facing copy).
 */
export const PHASE_RESULT_EDITOR_ID_PREFIXES = {
  issue: 'issue_',
  quickWin: 'quick_win_',
  recommendation: 'recommendation_',
} as const;
